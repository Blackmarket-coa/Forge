# Injected into the build so the Settings "Current version" badge is honest.
# Kept in lockstep with src-tauri/Cargo.toml + tauri.conf.json by
# scripts/check-version-sync.sh.
REACT_APP_VERSION=0.1.0
